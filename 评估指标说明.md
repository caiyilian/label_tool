# 项目目标与评估指标说明

## 一、终极目标

借助 AI 技术对教室监控视频进行智能分析，通过学生课堂坐姿量化监测，**探索班级行为与近视发展的关联性**。

## 二、当前阶段目标（初步目标）

以班级为单位，统计整体的标准/不良坐姿率，找到班级行为与近视的关系。

**不是**做单个学生追踪，**不是**做课间行为识别。

### 输出内容

- 每节课班级整体标准/不良坐姿率
- 每天班级整体标准/不良坐姿趋势
- 每周/每月班级整体标准/不良坐姿统计报表

## 三、坐姿判定规则

### 3.1 采样策略

```
一节课 40 分钟
每 10 分钟做 1 次抽检 → 共 4 次抽检
每次抽检持续 1 分钟
每隔 10 秒采集 1 张 → 共 6 张
```

**抽帧工具参数：** t1=10（采样周期，分钟）, t2=1（窗口时长，分钟）, t3=10（采集间隔，秒）

### 3.2 学生级坐姿不良判定

```
在一个 1 分钟窗口（6 帧）内：
  - 坐姿不良 ≥ 3 帧 → 认定该学生该窗口为「坐姿不良」
  - 坐姿不良 < 3 帧 → 认定该学生该窗口为「坐姿正常」
```

**核心逻辑：** 动作持续才算一次不良，偶发低头/歪头不算。3/6 阈值意味着该学生在该窗口内超过一半时间处于不良姿态。

### 3.3 班级级坐姿不良率

```
该窗口班级不良率 = 该窗口被判定为不良的学生数 / 全班学生总数
整节课班级不良率 = 4 个窗口不良率的平均值
```

## 四、模型评估指标（与之前不同）

### 之前（错误的方向）

我们一直在测**单帧级别**的指标：
- Posture Acc = 模型判定单张裁剪图 normal/bad 的准确率（74.6%）
- 这个指标不能反映实际业务价值

### 现在（正确的方向）

| 评估层级 | 聚合规则 | 核心指标 | 说明 |
|:--------|:---------|:---------|:-----|
| **单帧** | 无 | 不需要单独评估 | 单帧错误会被窗口聚合平滑 |
| **学生窗口级** | 6帧中≥3帧不良=该窗口不良 | 窗口级判定准确率 | 关注"模型是否判对这个人这个窗口" |
| **班级级** | 不良学生数/全班人数 | **班级不良率误差** | 核心指标：模型算出的班级不良率与真实值差多少个百分点 |
| **课程级** | 4个窗口平均 | 整节课不良率误差 | 长期趋势的准确性 |

### 核心指标定义

**班级不良率误差（绝对误差）**：
```
error = |模型计算的班级不良率 - 真实班级不良率|
```

例如：真实班级不良率 32.5%，模型算出 30.2%，误差 = 2.3 个百分点。

### 为什么这个指标更合理

1. **聚合平滑单帧错误**：模型在单帧上可能有 25% 的判错率，但 6 帧聚合后（≥3/6 规则），错误会相互抵消一部分
2. **班级级聚合进一步平滑**：全班 30-40 人的结果平均后，个体误差被进一步抵消
3. **最终产出是班级报表**：业务方看到的是"3班今日不良率 28.5%"，不是"某某学生某帧是否不良"

## 五、当前阶段不做的事

| 内容 | 原因 |
|:----|:-----|
| 课间行为识别 | 课间转头走动不算不良坐姿，且课间情况复杂 |
| 单个学生追踪 | 初步目标只需班级级统计，进阶目标才需要 |
| 单帧级精细优化 | 单帧准确率不是最终目标，窗口聚合才是 |

## 六、实际数据验证（基于当前测试集）

虽然当前数据没有完整的 6 帧窗口，但可以用**学生级聚合**（该学生所有帧中 ≥50% 不良 → 该学生不良）来近似验证班级级误差。

### 6.1 测试集结果（ResNet-18, 端到端管线）

| 班级 | 单帧准确率 | 单帧不良率误差 | **学生级不良率误差** |
|:----|:---------:|:-------------:|:------------------:|
| 12.02-305 | 80.6% | 5.2 百分点 | **3.1 百分点** |
| 12.04-402 | 71.7% | 2.7 百分点 | **5.7 百分点** |
| **两班合计** | **76.0%** | **1.1 百分点** | — |

### 6.2 关键结论

**单帧准确率 76% 时，班级不良率误差约 1-6 百分点。** 聚合显著平滑了单帧错误。

### 6.3 模拟推算（待真实窗口数据验证）

基于单帧准确率 76%，对 30 人班级模拟推算：

| 聚合层级 | 平均绝对误差 | 90% 分位误差 |
|:---------|:----------:|:-----------:|
| 单窗口班级不良率 | ~2 百分点 | ~4 百分点 |
| **整节课（4窗口平均）** | **~3.6 百分点** | **~7.5 百分点** |
| 每天（4节课平均） | ~2 百分点 | ~4 百分点 |

### 6.4 不同单帧准确率下的班级误差对比（30人班，模拟）

| 单帧准确率 | 每节课班级不良率平均误差 | 90% 分位误差 |
|:---------:|:---------------------:|:-----------:|
| 70% | ~4.5 百分点 | ~9 百分点 |
| 75% | ~3.6 百分点 | ~7.5 百分点 |
| 80% | ~3.0 百分点 | ~6.5 百分点 |
| 85% | ~2.3 百分点 | ~5.0 百分点 |
| 90% | ~1.7 百分点 | ~4.0 百分点 |

**结论：** 单帧准确率从 70% 提升到 90%，班级级误差从 4.5 百分点降到 1.7 百分点。当前 76% 的水平（误差 ~3.5 百分点）对于"班级行为与近视关联性"的初步探索阶段是可用的。

## 七、当前标注数据与评估的差距

当前标注数据（坐姿视频标记-20260722）的问题：
- 每班只有 6-10 帧，且帧号稀疏（如 [35, 79, 161, 169, 172, 175]）
- 无法形成连续的 1 分钟窗口（6 帧连续）
- 无法按"≥3/6"规则做窗口级评估

**需要重新抽帧：** 按 t1=10min, t2=1min, t3=10s 参数抽出完整的 4 个窗口 × 6 帧/窗口 = 24 帧/班/节课，标注人员按窗口完整标注。

## 八、文件名与窗口分组

### 7.1 部署流程

```
拿到一段完整视频
  ↓
用相同的采样配置（t1=10, t2=1, t3=10）抽帧
  ↓
得到按时间顺序编号的图片文件（front_000001.jpg, front_000002.jpg ...）
  ↓
将图片按窗口分组：每 6 帧一组，对应一个 1 分钟窗口
  ↓
模型对每帧预测每个学生的坐姿标签
  ↓
窗口内聚合：6 帧中 ≥3 帧不良 → 该学生该窗口不良
  ↓
班级聚合：计算该窗口的班级不良率
```

### 7.2 文件名的作用

图片文件名中的数字（如 `front_000035.jpg` 中的 35）是**抽帧序号**，它直接告诉我们：

- 该帧属于哪个窗口（窗口号 = 帧序号 // 6）
- 该帧在窗口内的位置（帧序号 % 6）
- 同一窗口内的 6 帧序号连续 → 自然形成一组

**这不是实时处理，而是后处理**：拿到所有抽帧图片后，按文件名分组，再逐窗口计算班级不良率。

### 7.3 与之前理解的区别

| 之前（错误） | 现在（正确） |
|:------------|:------------|
| 拿到单张图片，模型独立判断 | 拿到一个窗口（6帧）的图片，模型逐帧判断后聚合 |
| 评估单帧准确率 | 评估窗口聚合后的班级不良率 |
| 不需要文件名信息 | **文件名是分组的关键依据** |

## 九、模型评估流程（待实现）

```
输入：一节课的前视角视频 + 后视角视频
  ↓
按 t1=10, t2=1, t3=10 抽帧 → 4个窗口 × 6帧 = 24帧
  ↓
模型预测每帧每个学生的 bbox + 坐姿标签
  ↓
窗口聚合：对每个学生，6帧中≥3帧不良 → 该窗口不良
  ↓
班级聚合：不良学生数 / 全班人数 → 该窗口班级不良率
  ↓
对比：模型班级不良率 vs 真实班级不良率（基于标注）
  ↓
输出：4个窗口 × N个班级 的误差统计
```

## 十、附录：抽帧工具参数说明

当前工具默认参数为 t1=5, t2=1, t3=10，与需求（t1=10）不符。需要修改为：

| 参数 | 含义 | 需求值 | 当前默认值 |
|:----|:-----|:------:|:---------:|
| t1 | 采样周期（分钟） | 10 | 5 |
| t2 | 窗口时长（分钟） | 1 | 1 |
| t3 | 采集间隔（秒） | 10 | 10 |

每节课 40 分钟 → 4 个窗口 × 6 帧 = 24 帧/节课。