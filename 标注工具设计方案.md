# 坐姿标注工具设计方案

## 1. 目标

开发一个 Web 端标注工具，对已抽好的帧图片进行逐帧标注。每个学生在每帧中需要标注的最终字段如下：

| 字段 | 类型 | 取值 |
|------|------|------|
| `student_id` | 文本 | 匿名编号，如 `S01`、`S02`，同一学生在整段视频中保持一致 |
| `bbox` | 坐标 | `[x1, y1, x2, y2]`，该学生在画面中的人体检测框 |
| `status` | 单选 | `sitting_listening` / `sitting_reading` / `standing` / `left_seat` |
| `posture` | 单选 | `normal` / `bad` |
| `bad_reason` | 多选 | `head_tilt` / `head_down` / `lean_forward` / `side_lean`（仅当 `posture=bad` 时填写） |
| `pose_keypoints` | 点位数组 | **暂不实现**，预留字段 |

---

## 2. 核心问题：如何选中一个学生？

### 2.1 结论：用框（BBox）

经过对比三种方案，**采用画框（Bounding Box）作为选中学生的方式**。

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **画框 (BBox)** | 直观、可同时作为检测标签、划定区域后标签面板自动弹出 | 每帧每个学生都要画，工作量中等 | **采用** |
| 点击标记 (Point) | 速度快 | 歧义大（密集座位点不准）、无法提供检测框给模型 | 不采用 |
| 先做姿态估计再分类 | 信息最丰富 | 技术门槛高、标注速度极慢、当前阶段不需要 | 不采用 |

### 2.2 理由

1. **框 = 选中 + 标签一体**：画完框后该学生即被"选中"，自动弹出标签面板，不需要额外的选中操作。
2. **框是模型训练的必要输入**：目标检测模型（如 YOLO）需要 bbox 作为监督标签，画框一举两得。
3. **框内区域可裁剪**：后续做姿态估计或精细分类时，可直接裁剪框内区域作为输入。
4. **框可辅助遮挡判断**：标注员看到框与框重叠时，可直接判断遮挡程度。

---

## 3. 标注工作流

### 3.1 整体流程

```
加载一帧图片
    ↓
标注员对该帧中每个可见学生依次执行：
    ① 画框 → ② 自动分配/选择 student_id → ③ 填写标签 → ④ 确认
    ↓
当前帧所有学生标注完毕 → 点击"下一帧" → 重复
```

### 3.2 单个学生的标注步骤（详细）

```
步骤 1：画框
    鼠标在画面上拖拽，画出一个矩形框，框住该学生的身体区域。
    框的范围：从头顶到臀部下方，尽量贴合身体轮廓。

步骤 2：分配 student_id
    首次出现的学生 → 系统自动分配新编号（S01, S02, ...）
    已有学生 → 从下拉列表中选择已有编号（或系统根据框位置自动建议）
    同一编号在整段视频中指向同一个学生。

步骤 3：填写标签面板（画框后自动弹出）
    ┌─────────────────────────────────┐
    │  学生: [S01 ▾]                  │
    │                                 │
    │  状态:                          │
    │  ○ 坐-听说  ○ 坐-读写           │
    │  ○ 站立     ○ 离席              │
    │                                 │
    │  坐姿:                          │
    │  ○ 正常     ○ 不良              │
    │                                 │
    │  不良原因（多选，仅坐姿=不良时）:  │
    │  □ 歪头  □ 低头  □ 俯身  □ 侧身 │
    │                                 │
    │  备注: [________]               │
    │                                 │
    │  [确认]  [删除此框]              │
    └─────────────────────────────────┘

步骤 4：确认
    点击"确认"后，该学生的框在画面上显示为带编号的标注框。
    点击已有标注框可重新编辑标签。
```

### 3.3 前后视角的处理方式

标注以**前视角（front）为主视角**，后视角（back）为辅助。

```
┌──────────────────────┬──────────────────────┐
│                      │                      │
│    前视角 (front)     │    后视角 (back)      │
│    【主标注区域】      │    【参考区域】        │
│                      │                      │
│    在此画框+填标签     │    仅查看，辅助判断    │
│                      │    可选：画对应框       │
│                      │                      │
└──────────────────────┴──────────────────────┘
```

- 前视角完成所有标注（画框 + 分类标签）。
- 后视角同步展示同一秒的对应帧，标注员可参照后视角判断遮挡和坐姿。
- 后视角的 bbox 为可选操作（后续阶段再补充）。

---

## 4. 快捷操作设计

为提升标注效率，设计以下快捷键和交互：

| 操作 | 方式 | 说明 |
|------|------|------|
| 画框 | 鼠标拖拽 | 在前视角画面上按住左键拖拽画矩形 |
| 编辑标签 | 点击已有框 | 点击已标注的框，弹出标签面板供修改 |
| 删除标注 | 右键菜单 或 面板按钮 | 右键点击框 → 删除，或在面板中点击"删除此框" |
| 复制上一帧 | 快捷键 `C` | 将上一帧的所有标注复制到当前帧（学生通常不会移动），然后微调 |
| 切换学生 | `Tab` | 在当前帧已有的标注之间快速切换选中 |
| 确认并下一个 | `Enter` | 确认当前标签面板，关闭面板 |
| 撤销 | `Ctrl+Z` | 撤销上一步画框或删除操作 |
| 上一帧 / 下一帧 | `←` / `→` | 在帧之间导航 |
| 缩放平移 | 滚轮缩放 + 左键拖拽（放大后） | 与现有抽帧工具的预览交互一致 |

---

## 5. 标签面板的智能行为

### 5.1 状态联动

```
当 status = "left_seat" 时：
    → posture 自动置灰，强制为 "normal"（离席不需要判断坐姿）
    → bad_reason 全部清空并置灰

当 status = "standing" 时：
    → posture 可选（站姿也可判断是否不良，如驼背）
    → bad_reason 仍然可用

当 status = "sitting_listening" 或 "sitting_reading" 时：
    → posture 正常可选
    → 当 posture = "normal" 时，bad_reason 自动清空并置灰
    → 当 posture = "bad" 时，bad_reason 解锁，至少选一项
```

### 5.2 默认值

| 字段 | 默认值 |
|------|--------|
| status | `sitting_listening` |
| posture | `normal` |
| bad_reason | 空（无） |

---

## 6. 帧导航与批量标注策略

### 6.1 帧导航

- 抽帧工具输出的图片按 `front_000001.jpg`, `front_000002.jpg`, ... 命名。
- 标注工具按文件名排序加载帧序列。
- 提供帧列表侧边栏，可快速跳转到任意帧。
- 已完成标注的帧在列表中打勾标记。

### 6.2 批量复制

- **复制上一帧（核心功能）**：课堂场景中学生位置和姿态变化缓慢，大部分帧与上一帧相同。标注员只需复制上一帧，然后微调有变化的学生。
- 这是提升效率的最关键功能。

---

## 7. 标注数据存储格式

### 7.1 每帧存储为一个 JSON 文件

文件命名：`front_000001.json`（与对应图片同名，后缀改为 `.json`）

```json
{
  "frame_id": "front_000001",
  "image_file": "front_000001.jpg",
  "class_id": "Grade7_Class02",
  "date": "2026-04-01",
  "real_timestamp": "2026-04-01 09:31:24",
  "period_state": "class",
  "annotations": [
    {
      "student_id": "S01",
      "bbox": [480, 220, 535, 390],
      "status": "sitting_reading",
      "posture": "bad",
      "bad_reason": ["head_down", "lean_forward"],
      "pose_keypoints": null,
      "note": ""
    },
    {
      "student_id": "S02",
      "bbox": [620, 240, 680, 410],
      "status": "sitting_listening",
      "posture": "normal",
      "bad_reason": [],
      "pose_keypoints": null,
      "note": ""
    }
  ]
}
```

### 7.2 学生注册表（全局）

文件命名：`students.json`，存储在输出目录根下。

```json
{
  "class_id": "Grade7_Class02",
  "students": [
    { "student_id": "S01", "seat_row": 3, "seat_col": 2 },
    { "student_id": "S02", "seat_row": 3, "seat_col": 3 },
    { "student_id": "S03", "seat_row": 4, "seat_col": 1 }
  ]
}
```

---

## 8. 页面布局设计

```
┌─────────────────────────────────────────────────────────────────┐
│  标注工具 - Grade7_Class02 - 09:31:24            帧 12 / 3600  │
├───────────────────────────────────────┬─────────────────────────┤
│                                       │                         │
│                                       │    帧列表               │
│         前视角 (主标注区)              │    ✅ 001               │
│         可缩放、可拖拽                 │    ✅ 002               │
│         在此画框                       │    ▶ 003  ← 当前       │
│                                       │       004               │
│                                       │       005               │
│                                       │       ...               │
├───────────────────────────────────────┤                         │
│                                       │    学生列表 (本帧)       │
│         后视角 (参考区)                │    S01 坐-读 不良        │
│         同步显示对应帧                 │    S02 坐-听 正常        │
│                                       │    S03 站立   正常       │
│                                       │                         │
├───────────────────────────────────────┴─────────────────────────┤
│  [← 上一帧]  [复制上一帧 ⌘C]  [下一帧 →]     [保存]  [导出全部]  │
└─────────────────────────────────────────────────────────────────┘
```

当点击某个学生框或学生列表项时，弹出标签填写浮窗：

```
┌──────────────────────────┐
│  学生: [S01 ▾]           │
│                          │
│  状态:                    │
│  ○ 坐-听说  ○ 坐-读写     │
│  ○ 站立     ○ 离席        │
│                          │
│  坐姿:                    │
│  ○ 正常     ○ 不良        │
│                          │
│  不良原因:                │
│  □ 歪头  □ 低头           │
│  □ 俯身  □ 侧身           │
│                          │
│  备注: [____________]    │
│                          │
│  [确认 ⏎]  [删除 🗑]     │
└──────────────────────────┘
```

---

## 9. 技术方案

| 层面 | 选型 | 理由 |
|------|------|------|
| 后端 | Flask (与现有工具复用) | 项目已有 Flask 基础，无需引入新框架 |
| 前端 | 原生 HTML/JS + Tailwind CSS | 与现有 Web 版风格一致，轻量无依赖 |
| 画框交互 | Canvas API | 原生 Canvas 绑定鼠标事件实现拖拽画框，性能好 |
| 图片加载 | 直接 `<img>` 加本地文件 | 标注工具在本地运行，图片路径直接访问 |
| 数据存储 | JSON 文件 | 简单直接，无需数据库，方便版本控制和后续处理 |

---

## 10. 开发计划

| 阶段 | 内容 | 说明 |
|------|------|------|
| **P0** | 基础画框 + 标签填写 | 在单张图片上能画框、弹出面板、填写并保存标签 |
| **P1** | 帧导航 + 复制上一帧 | 支持前后翻帧、复制上一帧标注、帧列表 |
| **P2** | 前后视角双屏 + 缩放拖拽 | 后视角参考区、图片缩放拖拽 |
| **P3** | 批量导出 + 学生注册表 | 导出全部标注为 JSON、管理学生编号 |

当前先实现 **P0**。

---

## 11. 开发历程与现状

### 11.1 开发历程

| 日期 | 里程碑 | 说明 |
|------|--------|------|
| 2026-05-12 | 抽帧工具完成 | `web_app.py` + `video_sync_tool.py` 实现多视角视频同步抽帧，支持打包为 exe |
| 2026-05-19 | 标注工具方案设计 | 完成 `标注工具设计方案.md`，确定画框选中 + 分类标签的技术路线 |
| 2026-05-19 | 标注工具 P0~P2 实现 | 完成 `annotation_app.py` + `annotation.html`，实现全部核心功能 |
| 2026-05-19 | 交互优化迭代 | 修复重置视图、撤销重做、右键菜单、编号管理、排序等问题 |

### 11.2 已完成的功能

**抽帧工具**（`web_app.py` / `video_sync_tool.py`）
- 前后视角视频选择与时间对齐
- ffmpeg 逐秒抽帧，双视角帧数严格一致
- 分率限制、损坏视频智能时长估算
- PyInstaller 打包为独立 exe

**标注工具**（`annotation_app.py` + `templates/annotation.html`）
- 文件夹选择与校验（forward/backward 子目录、图片数量一致性）
- 前后视角双屏展示，均支持滚轮缩放 + 中键拖拽平移
- Canvas 画框标注：拖拽画框、拖拽移动、角点调整大小、双击编辑
- 标签面板：状态（坐听/坐读/站/离席）、坐姿（正常/不良）、不良原因（多选）
- 学生编号管理：自动分配、删除后重新编号保持连续、编辑编号时冲突自动交换
- 帧导航：上一帧/下一帧、帧列表跳转、已标注帧标记
- 复制上一帧：核心效率功能
- 撤销/重做：Ctrl+Z / Ctrl+Y，支持画框、删除、移动、复制等操作
- 右键菜单：编辑标签、上移、下移、删除标注、重置视图（根据上下文动态显示/隐藏）
- 学生列表：上下箭头调整排序，禁用状态与位置一致
- 重置视图：恢复图片初始位置和大小
- 标注数据以 JSON 格式存储在 `annotations/` 子目录

### 11.3 文件结构

```
label_tool/
├── annotation_app.py          # 标注工具后端 (Flask, 端口 5001)
├── templates/
│   ├── annotation.html        # 标注工具前端
│   └── index.html             # 抽帧工具前端
├── web_app.py                 # 抽帧工具后端 (Flask, 端口 5000)
├── video_sync_tool.py         # 抽帧工具桌面版 (Tkinter)
├── 多视角学生行为时序识别模型方案.md
├── 标注工具设计方案.md         # 本文档
└── ...
```

### 11.4 运行方式

```bash
# 抽帧工具
python web_app.py

# 标注工具
python annotation_app.py
```

---

## 12. 下一步计划

### 12.1 近期（P3）

- **批量导出功能**：将所有帧的标注合并导出为统一的 JSON/CSV 文件，便于后续模型训练
- **学生注册表管理**：在标注工具中维护全局 `students.json`，记录学生座位信息
- **标注统计面板**：显示已标注帧数、未标注帧数、各状态分布等

### 12.2 中期

- **后视角标注**：在后视角也支持画框，建立前后视角的学生对应关系
- **标注质量校验**：检测漏标、标注异常（如框过小/过大）、前后视角帧数不一致等
- **快捷键优化**：数字键快速切换状态（1=坐听、2=坐读、3=站、4=离席）

### 12.3 远期

- **姿态估计标注**：启用 `pose_keypoints` 字段，标注人体关键点
- **多人协作标注**：支持多人同时标注不同帧段，合并结果
- **AI 辅助预标注**：用检测模型自动框选学生，人工只需修正和填写标签
